{
  "name": "@molt/health",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure drift-detection core. Zero I/O: rows in, health verdict out.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json"
  }
}
